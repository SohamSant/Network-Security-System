# Network Security System

This project is a Network Security System.

## Setup

1. Create a conda environment:
   ```bash
   conda create -n venv python=3.10
   conda activate venv
   ```
2. Install requirements:
   ```bash
   pip install -r requirements.txt
   ```
